# Insurance-Premium-Prediction-API
This project was built to learn and implement FastAPI with a machine learning model. The model was trained on a toy dataset, as the primary goal of the project was to understand how to integrate and deploy an ML model using FastAPI, rather than to build a production-grade ML model.
